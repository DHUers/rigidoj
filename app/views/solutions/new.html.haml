- content_for :title, 'Submit'

.row
  .col-md-10
    = form_for @solution, html: { role: 'form', class: 'solution-form' } do |f|
      = render partial: 'form', locals: { f: f }
      = f.button 'Submit', class: 'btn btn-success'
