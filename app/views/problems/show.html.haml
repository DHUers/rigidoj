- content_for :title, @problem.title

- if current_user && current_user.staff?
  #problem-admin.admin-wrench.btn-group
    %button{type: 'button', class: 'btn btn-default dropdown-toggle',
        data: {toggle: 'dropdown'}, aria: {expanded: 'false'}}
      %i.fa.fa-wrench
    %ul.dropdown-menu.pull-right{role: 'menu'}
      %li
        %a{href: edit_problem_path(@problem)}
          Edit problem

.row
  .col-xs-12
    .page-header
      %h1.problem-title
        = @problem.id
        = @problem.title
.row
  .col-xs-9.problem-body
    .meta
      .judge-limits
        - @problem.judge_limits.each do |platform, limit|
          .row.judge-limit-group
            .col-xs-2
              = platform
            .col-xs-10
              %span.time-limit= limit[:time]
              %span.memory-limit= limit[:memory]
    :preserve
      #{raw @problem.baked}
  .col-xs-3
    %ul.action-list.problem-actions.sticky
      %li
        %a{href: new_problem_solution_path(@problem)}
          %i.fa.fa-plus-circle
          Submit
      %li
        %a{href: problem_solutions_path(@problem)}
          %i.fa.fa-history
          Submitted records
