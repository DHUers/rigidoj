- content_for :title, @problem.title

.row
  .col-xs-12
    .page-header
      %h1.problem-title
        = @problem.id
        = @problem.title
.row
  .col-xs-9
    .problem-body
      :preserve
        #{raw @problem.baked}
  .col-xs-3
    %ul.actions.problem-actions
      %li= link_to 'Submit', new_problem_solution_path(@problem)
      %li= link_to 'Submitted records', problem_solutions_path(@problem)
    - if current_user && current_user.staff?
      .divider
      %ul.actions.problem-admin-actions
        %li= link_to 'Edit', edit_problem_path(@problem)
