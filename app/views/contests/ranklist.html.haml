- content_for :title, "#{@contest.title} ranklist"

.panel.panel-info
  .panel-heading
    = @contest.title
  %table.table.table-hover.table-bordered.table-condensed
    %thead
      %tr
        %th
    %tbody
      %tr
