- content_for :title, "#{@contest.title} ranking"

.panel.panel-info
  .panel-heading
    = @contest.title
  %table.table.table-hover.table-bordered.table-condensed
    %thead
      %tr
        %th
    %tbody
      %tr
